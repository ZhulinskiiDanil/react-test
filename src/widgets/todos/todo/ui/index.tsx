import styles from './index.module.scss';import { memo } from 'react';import { Todo as TodoType } from '@/features/todos/types';// Featuresimport { DeleteTodoButton } from '@/features/todos/deleteTodoButton/ui';import { UpdateTodoButton } from '@/features/todos/updateTodoButton';import { CompleteCheckbox } from '@/features/todos/completeCheckbox/ui';export const Todo = memo(({ todo }: { todo: TodoType }) => {  return (    <article key={todo.id} className={styles.todo}>      <label className={styles.label}>        <CompleteCheckbox todo={todo} />        <span className={styles.name}>{todo.title}</span>      </label>      <div className={styles.buttons}>        <DeleteTodoButton id={todo.id} />        <UpdateTodoButton todo={todo} />      </div>    </article>  );});Todo.displayName = 'Todo';