import { UIButton } from '@/shared/ui';import { todosApi } from '@/features/store/api/todos/todos.api';import { Todo } from '@/features/todos/types';export function UpdateTodoButton({ todo }: { todo: Todo }) {  const [deleteTodo] = todosApi.useDeleteTodoMutation();  const [updateTodo] = todosApi.useUpdateTodoMutation();  return (    <UIButton      variant="secondary"      onClick={() => {        const title = prompt('Enter new todo title', todo.title);        if (title) {          updateTodo({ ...todo, title });        } else if (title !== null) {          if (confirm('Вы уверены что хотите удалить todo?')) {            deleteTodo({ id: todo.id });          }        }      }}>      Update todo    </UIButton>  );}